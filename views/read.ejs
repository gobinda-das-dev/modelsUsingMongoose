<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Read Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    
  <div class="h-full w-full bg-zinc-900 text-white">
    <nav class="p-10 text-sm text-blue-500">
      <a href="/">Back to home</a>
    </nav>

    <div class="p-10 pt-0 space-y-5">
      <h1 class="text-teal-500 font-semibold text-2xl">All Users</h1>

      <div class="users flex gap-3 flex-wrap">
        <% users.forEach(user => { %>
          <div class="user w-52 bg-zinc-800 p-3 rounded">
            <div class="w-full h-52 rounded bg-zinc-700 overflow-hidden">
              <img class="size-full object-cover" src="<%= user.image %>" alt="">
            </div>
            <h2 class="mt-3 font-semibold text-xl"><%= user.name %></h2>
            <h3 class="text-zinc-400 text-sm"><%= user.email %></h3>
            <div class="mt-5 space-x-5">
              <a class="text-sm text-zinc-200" href="/edit/<%= user._id %>">Edit User</a>
              <a class="text-sm text-red-400" href="/delete/<%= user._id %>">Delete User</a>
            </div>
          </div>
        <% }) %>
        
        
        <!-- <div class="user w-52 bg-zinc-800 p-3 rounded">
          <div class="w-full h-32 rounded bg-zinc-700 overflow-hidden">
            <img class="size-full object-cover" src="https://th.bing.com/th/id/OIG4.LgUj9FIjzUbdTSMn0mRg" alt="">
          </div>
          <h2 class="mt-3 font-semibold text-xl">Gobinda Das</h2>
          <div class="mt-5 space-x-5">
            <a class="text-sm text-zinc-200" href="">Edit User</a>
            <a class="text-sm text-red-400" href="">Delete User</a>
          </div>
        </div> -->
      </div>
    </div>
  </div>
  
    <script src="script.js"></script>
</body>
</html>